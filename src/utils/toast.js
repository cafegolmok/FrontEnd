// // src/utils/toast.js

// let toastId = 0;

// export const showToast = (setToasts, message) => {
//   const id = toastId++;

//   // 기존 토스트제거
//   setToasts([]);

//   setToasts(toasts => [...toasts, { id, message }]);
// };
